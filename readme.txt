Основные изменения находятся в диалогс и профиле их контейнера
в майпост я имортировал стор как заглушку, потому что это не контейнер а там есть .мар который стор просит(потом рефакторить надо)
значит удалены по сути все пропсы, использвалось сознание контеста 

export const StoreContext = React.createContext(store) надо было вместо стора писать налл, но и так работает

<StoreContext.Provider value ={то что передать надо}> применяется к парент компоненте(индекс) и получается что через 
<StoreContext.Consumer> применённой в чилдрен компоненте передаётся стор(вернее даёт возможность её использовать)
в презентационных функциях проспы остались
в <StoreContext.Consumer>
{  (store) => {
		... какие-то функции/коллбэки(ЭкшнКреэйт например туды сюды)
		return( 
			<Компонента ... пропсы какие-то/>
			)
	      }
} 
</StoreContext.Consumer>
   в одной строке с <StoreContext.Consumer> не должно быть ничего, скобка должна быть на следующей строке
шоб всё работало

Опять же с типизацией проблем вроде бы не возникло но хз типизирует всё сам вроде бы
